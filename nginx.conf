server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    # React Router 대응: 모든 경로를 index.html로 연결
    # 이게 없으면 /notes/1 같은 경로에서 새로고침 시 404 발생
    location / {
        try_files $uri $uri/ /index.html;
    }
}